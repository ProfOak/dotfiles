conky.config = {
    out_to_console = true,
    out_to_x = false,
    background = false,
    update_interval = 2,
    total_run_times = 0,
    use_spacer = 'none',

	--override_utf8_locale = true,
    times_in_seconds =  true,
    default_colors= '#123456'
};

-- mpd > volume > battery > date > time
conky.text = [[
${color1} $mpd_smart ${color2} ${battery_percent}% ${color3}(${format_time $battery_time "\hh\mm"}) ${color4}${time  %m/%d} ${color5}${time  %I:%M %P}
]];
